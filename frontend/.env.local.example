# Copy to .env.local and adjust. Next.js loads .env.local automatically.

# Backend API base URL — must include the /api prefix (matches FastAPI mount in main.py).
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Optional label for environment (e.g. development, production).
NEXT_PUBLIC_APP_ENV=development
